name: Windows x64 Build

on:
  push:
    branches: [ dev ]
  workflow_dispatch:

concurrency:
  group: windows-build
  cancel-in-progress: true

jobs:
  build-windows:
    runs-on: windows-2022
    defaults:
      run:
        shell: cmd
    steps:
    - name: Prepare directories
      run: |
        mkdir %GITHUB_WORKSPACE%\TBuild\Libraries\win64
        mkdir %GITHUB_WORKSPACE%\TBuild\ThirdParty

    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: recursive
        path: TBuild\AyuGramDesktop

    - uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - uses: dtolnay/rust-toolchain@stable

    - uses: Eden-CI/msvc-dev-cmd@master
      name: Setup MSVC
      with:
        arch: x64
        toolset: '14.44'

    - name: Prepare cache key
      shell: bash
      run: |
        PREPARE_HASH=$(sha256sum TBuild/AyuGramDesktop/Telegram/build/prepare/prepare.py | awk '{ print $1 }')
        echo "CACHE_KEY=$PREPARE_HASH" >> $GITHUB_ENV

    - name: Cache ThirdParty
      id: cache-thirdparty
      uses: actions/cache@v4
      with:
        path: TBuild\ThirdParty
        key: thirdparty-win64-rel-${{ env.CACHE_KEY }}
        restore-keys: thirdparty-win64-rel-

    - name: Cache Libraries
      id: cache-libs
      uses: actions/cache@v4
      with:
        path: TBuild\Libraries
        key: libs-win64-rel-${{ env.CACHE_KEY }}
        restore-keys: libs-win64-rel-

    - name: Cache Qt
      id: cache-qt
      uses: actions/cache@v4
      with:
        path: |
          TBuild\Libraries\qt*
          TBuild\Libraries\Qt*
        key: qt-win64-rel-${{ env.CACHE_KEY }}
        restore-keys: qt-win64-rel-

    - name: Prepare Libraries
      run: |
        cd %GITHUB_WORKSPACE%\TBuild\AyuGramDesktop\Telegram\build\prepare
        python prepare.py silent

    - name: Configure
      run: |
        cd %GITHUB_WORKSPACE%\TBuild\AyuGramDesktop\Telegram
        call configure.bat x64 -D TDESKTOP_API_ID=611335 -D TDESKTOP_API_HASH=d524b414d21f4d37f08684c1df41ac9c -D CMAKE_CONFIGURATION_TYPES=Release -D DESKTOP_APP_DISABLE_CRASH_REPORTS=ON

    - name: Build
      run: |
        cd %GITHUB_WORKSPACE%\TBuild\AyuGramDesktop\Telegram
        cmake --build ..\out --config Release --parallel 4

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: AyuGram-Windows-x64
        path: TBuild\AyuGramDesktop\out\Release\AyuGram.exe
        retention-days: 7
